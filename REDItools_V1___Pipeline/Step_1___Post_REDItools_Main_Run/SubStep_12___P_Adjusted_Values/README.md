# SubStep 12: P Adjusted Values

## Scaffold Protocol For SubStep 12: P-Adjusted Values

1) Open the `ipynb` in SubStep 12 P-Adjusted Values and modify it to have the following inputs per pre-filtration:

| Variables | Description |
|----------|----------|
| `input_tsv`    | Path to the `.tsv` file generated in the previous sub-step   | 
| `output_directory`    | Path to the SubStep 12 directory pre-filtration folder   | 
| `Output_File_BaseName`    | An output file prefix of your choice   |

2) Run the `.ipynb`. 
